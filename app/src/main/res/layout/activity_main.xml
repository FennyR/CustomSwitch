<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Switch
        android:id="@+id/native_switch"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/nativeSwitchTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/native_switch"
        android:layout_centerHorizontal="true"
        android:textColor="@color/tvColor"
        android:textSize="12sp"
        android:layout_marginTop="10dp"/>

    <Switch
        android:id="@+id/customSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/nativeSwitchTv"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:thumb="@drawable/switch_selector"
        android:track="@drawable/switch_bar" />

    <TextView
        android:id="@+id/customSwitchTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/customSwitch"
        android:layout_centerHorizontal="true"
        android:textColor="@color/tvColor"
        android:textSize="12sp"
        android:layout_marginTop="10dp"/>
</RelativeLayout>
